# wf-workspace configuration
#
# Config lives in ~/.config/wf-workspace/
#
# Global config: ~/.config/wf-workspace/config
# Projects:      ~/.config/wf-workspace/projects/<name>
#
# Run `wf-workspace` for the first time to set up interactively,
# or `wf-workspace setup <project-name>` to add a project.

# --- ~/.config/wf-workspace/config ---
# BRANCH_PREFIX="yourname"
# DEFAULT_PROJECT="wefunder"

# --- ~/.config/wf-workspace/projects/wefunder ---
# REPO="/path/to/wefunder"
# SESSION="wefunder"
# WORKTREE_BASE="/path/to/worktrees"    # optional: omit for direct repo tabs
# DEV_CMD="bin/dev-worktree start"      # optional: omit for no dev server pane

# --- ~/.config/wf-workspace/projects/wf-workspace ---
# REPO="/path/to/wf-workspace"
# SESSION="wf-workspace"
# (no WORKTREE_BASE or DEV_CMD = simple claude + tig layout)
